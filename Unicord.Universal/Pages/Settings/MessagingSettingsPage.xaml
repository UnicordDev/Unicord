<Page
    x:Class="Unicord.Universal.Pages.Settings.MessagingSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unicord.Universal.Pages.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Page.DataContext>
        <local:MessagingSettingsModel />
    </Page.DataContext>

    <StackPanel>
        <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Text="Text and Images" Margin="0,0,0,12"/>

        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Text="Spoilers"/>
        <Grid Grid.Row="1" Margin="0,4,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Enable Spoilers" VerticalAlignment="Center"/>
            <ToggleSwitch Grid.Column="1" Style="{StaticResource NoTextToggleSwitchStyle}" IsOn="{Binding EnableSpoilers, Mode=TwoWay}" />
        </Grid>
        
    </StackPanel>
</Page>
