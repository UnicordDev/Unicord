<Page
    x:Class="Unicord.Universal.Pages.Settings.SecuritySettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unicord.Universal.Pages.Settings"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.Toolkit.Uwp.UI.Controls"
    mc:Ignorable="d" Loaded="Page_Loaded" x:DefaultBindMode="TwoWay">
    <Page.DataContext>
        <local:SecuritySettingsModel/>
    </Page.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock Style="{StaticResource SubheaderTextBlockStyle}" Text="Security and Privacy"/>
        
        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Grid.Row="1" Margin="0,8,0,0" Text="Diagnostic and Usage data"/>

        <Grid Grid.Row="2" Margin="0,4,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>

            <TextBlock Text="Send Diagnostic and Usage data" VerticalAlignment="Center"/>
            <ToggleSwitch Toggled="ToggleSwitch_Toggled" Grid.Column="1" Style="{StaticResource NoTextToggleSwitchStyle}" IsOn="{Binding EnableAnalytics, Mode=TwoWay}" />
        </Grid>
        
        <TextBlock Style="{StaticResource SubtitleTextBlockStyle}" Grid.Row="3" Text="Windows Hello"/>

        <TextBlock x:Name="unavailableText" Grid.Row="4" Margin="0,12,0,0" TextWrapping="Wrap" Foreground="{StaticResource ErrorTextForegroundBrush}">
            <Run Text="Windows Hello is not available on this device. Wanna"/> <Hyperlink NavigateUri="ms-settings:signinoptions"><Run>change this?</Run></Hyperlink>
        </TextBlock>
        <ContentControl x:Name="settingsContent" Grid.Row="5" Margin="0,12" HorizontalContentAlignment="Stretch">
            <StackPanel>
                <TextBlock Text="Require Windows Hello authentication to:" TextWrapping="Wrap"/>

                <Grid Margin="0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Text="Login" VerticalAlignment="Center"/>
                    <ToggleSwitch Style="{StaticResource NoTextToggleSwitchStyle}" Grid.Column="1" IsOn="{Binding HelloForLogin, Mode=TwoWay}" />

                    <TextBlock Grid.Row="1" Text="Change settings" VerticalAlignment="Center"/>
                    <ToggleSwitch Grid.Row="1"  Grid.Column="1" Style="{StaticResource NoTextToggleSwitchStyle}" IsOn="{Binding HelloForSettings, Mode=TwoWay}" />

                    <TextBlock Grid.Row="2" Text="Access NSFW channels" VerticalAlignment="Center"/>
                    <ToggleSwitch Grid.Row="2"  Grid.Column="1" Style="{StaticResource NoTextToggleSwitchStyle}" IsOn="{Binding HelloForNSFW, Mode=TwoWay}" />

                </Grid>

                <TimePicker Time="{Binding AuthenticationTime, Mode=TwoWay}" Header="Keep me authenticated for" HorizontalAlignment="Stretch" />
            </StackPanel>
        </ContentControl>


        <TextBlock Grid.Row="6" Grid.ColumnSpan="2"><Hyperlink>Privacy Policy</Hyperlink></TextBlock>

    </Grid>
</Page>
