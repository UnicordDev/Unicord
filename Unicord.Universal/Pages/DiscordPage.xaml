<Page
    x:Name="self"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Unicord.Universal.Pages"
    xmlns:dis="using:Unicord.Universal"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Unicord.Universal.Controls"
    xmlns:sub="using:Unicord.Universal.Pages.Subpages"
    xmlns:media="using:Microsoft.Toolkit.Uwp.UI.Media"
    xmlns:discord="using:DSharpPlus.Entities"
    xmlns:lib="using:Microsoft.UI.Xaml.Controls"
    xmlns:controls1="using:Microsoft.Toolkit.Uwp.UI.Controls"
    xmlns:settings="using:Unicord.Universal.Pages.Settings"
    x:Class="Unicord.Universal.Pages.DiscordPage"
    mc:Ignorable="d" Loaded="Page_Loaded" Unloaded="Page_Unloaded">
    <Page.Resources>
        <CircleEase EasingMode="EaseInOut" x:Key="Ease"/>
        <Storyboard x:Key="OpenSettingsDesktopStoryboard" x:Name="OpenSettingsDesktopStoryboard">
            <DoubleAnimation 
                To="0"
                From="450"
                Storyboard.TargetName="SettingsPaneTransform"
                Storyboard.TargetProperty="X"
                Duration="00:00:00.50"
                EasingFunction="{StaticResource Ease}"/>
            <DoubleAnimation 
                To="0"
                Storyboard.TargetName="SettingsPaneTransform"
                Storyboard.TargetProperty="Y" Duration="00:00:00" />
            <DoubleAnimation
                To="1"
                Storyboard.TargetName="SettingsOverlayBackground"
                Storyboard.TargetProperty="Opacity"
                Duration="00:00:00.50"                            
                EasingFunction="{StaticResource Ease}"/>
        </Storyboard>
        <Storyboard x:Key="OpenSettingsMobileStoryboard" x:Name="OpenSettingsMobileStoryboard">
            <DoubleAnimation 
                To="0"
                From="2048"
                Storyboard.TargetName="SettingsPaneTransform"
                Storyboard.TargetProperty="Y"
                Duration="00:00:00.50"
                EasingFunction="{StaticResource Ease}"/>
            <DoubleAnimation 
                To="0"
                Storyboard.TargetName="SettingsPaneTransform"
                Storyboard.TargetProperty="X" Duration="00:00:00" />
            <DoubleAnimation 
                To="1"
                Storyboard.TargetName="SettingsOverlayBackground"
                Storyboard.TargetProperty="Opacity"
                Duration="00:00:00.50"
                EasingFunction="{StaticResource Ease}"/>
        </Storyboard>
        <Storyboard x:Key="CloseSettingsDesktopStoryboard" x:Name="CloseSettingsDesktopStoryboard" Completed="CloseSettingsStoryboard_Completed">
            <DoubleAnimation 
                From="0"
                To="450"
                Storyboard.TargetName="SettingsPaneTransform"
                Storyboard.TargetProperty="X"
                Duration="00:00:00.33"
                EasingFunction="{StaticResource Ease}"/>
            <DoubleAnimation 
                To="0"
                Storyboard.TargetName="SettingsPaneTransform"
                Storyboard.TargetProperty="Y" Duration="00:00:00" />
            <DoubleAnimation 
                To="0"
                Storyboard.TargetName="SettingsOverlayBackground"
                Storyboard.TargetProperty="Opacity"
                Duration="00:00:00.33"                            
                EasingFunction="{StaticResource Ease}"/>
        </Storyboard>
        <Storyboard x:Key="CloseSettingsMobileStoryboard" x:Name="CloseSettingsMobileStoryboard" Completed="CloseSettingsStoryboard_Completed">
            <DoubleAnimation 
                x:Name="MobileHeightAnimation"
                From="0"
                To="2048"
                Storyboard.TargetName="SettingsPaneTransform"
                Storyboard.TargetProperty="Y"
                Duration="00:00:00.33"
                EasingFunction="{StaticResource Ease}"/>
            <DoubleAnimation 
                To="0"
                Storyboard.TargetName="SettingsPaneTransform"
                Storyboard.TargetProperty="X" Duration="00:00:00" />
            <DoubleAnimation 
                To="0"
                Storyboard.TargetName="SettingsOverlayBackground"
                Storyboard.TargetProperty="Opacity"
                Duration="00:00:00.33"                            
                EasingFunction="{StaticResource Ease}"/>
        </Storyboard>

        <Storyboard x:Key="OpenPaneMobileStoryboard" x:Name="OpenPaneMobileStoryboard">
            <DoubleAnimation 
                To="276"
                Storyboard.TargetName="ContentTransform"
                Storyboard.TargetProperty="X"
                Duration="00:00:00.33"
                EasingFunction="{StaticResource Ease}"/>
        </Storyboard>
        <Storyboard x:Key="ClosePaneMobileStoryboard" x:Name="ClosePaneMobileStoryboard">
            <DoubleAnimation 
                To="0"
                Storyboard.TargetName="ContentTransform"
                Storyboard.TargetProperty="X"
                Duration="00:00:00.33"
                EasingFunction="{StaticResource Ease}"/>
        </Storyboard>


    </Page.Resources>
    <Grid>
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState x:Name="WideState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="769" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="ContentTransform.X" Value="0" />
                        <Setter Target="closeItem.Visibility" Value="Collapsed" />
                        <Setter Target="content.Margin" Value="0,0,0,0" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="NarrowState">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0" />
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="closeItem.Visibility" Value="Visible" />
                        <Setter Target="content.Margin" Value="-276,0,0,0" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>

        <Grid x:Name="grid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid x:Name="splitPane" Width="276" HorizontalAlignment="Left">

                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="56"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid x:Name="sidebarMainGrid" Background="{ThemeResource SidebarPrimaryAcrylicWindowBrush}">

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid x:Name="iconGrid" Visibility="Collapsed" Height="42">
                        <Image Source="{StaticResource DiscordClyde}" Width="22" Height="22" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Grid>

                    <ListView x:Name="guildsList" ItemTemplate="{StaticResource GuildsListTemplate}" Grid.Row="1" ShowsScrollingPlaceholders="False" ItemsSource="{x:Bind _guilds}" CanDrag="True" AllowDrop="True" CanDragItems="True" CanReorderItems="True" DragItemsCompleted="guildsList_DragItemsCompleted" ScrollViewer.VerticalScrollBarVisibility="Hidden" SelectionChanged="GuildsList_SelectionChanged">
                        <ListView.Header>
                            <StackPanel Margin="0,8,0,0">
                                <ListViewItem x:Name="closeItem" Tapped="CloseItem_Tapped" Padding="0" MinWidth="0" HorizontalContentAlignment="Stretch">
                                    <FontIcon Width="36" Height="36" Glyph="&#xE700;"></FontIcon>
                                </ListViewItem>
                                <ListViewItem x:Name="friendsItem" Tapped="friendsItem_Tapped" Padding="0" MinWidth="0" HorizontalContentAlignment="Stretch">
                                    <SymbolIcon Width="36" Height="36" Symbol="People"></SymbolIcon>
                                </ListViewItem>

                                <ListView x:Name="unreadDms" SelectionChanged="UnreadDms_SelectionChanged" ScrollViewer.VerticalScrollMode="Disabled">
                                    <ListView.HeaderTemplate>
                                        <DataTemplate>
                                            <lib:NavigationViewItemSeparator/>
                                        </DataTemplate>
                                    </ListView.HeaderTemplate>
                                    <ListView.ItemContainerStyle>
                                        <Style TargetType="ListViewItem">
                                            <Setter Property="Padding" Value="0"/>
                                            <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                                        </Style>
                                    </ListView.ItemContainerStyle>
                                    <ListView.ItemTemplate>
                                        <DataTemplate>
                                            <Grid>
                                                <lib:PersonPicture Margin="10,4" Width="36" Height="36" DisplayName="{Binding Converter={StaticResource DMNameConverter}}" ProfilePicture="{Binding Converter={StaticResource DMIconConverter}}"/>
                                                <Grid x:Name="unreadIndicator" MinWidth="4" Margin="0,4" HorizontalAlignment="Left" VerticalAlignment="Stretch" Background="{ThemeResource SystemControlForegroundAccentBrush}">
                                                    <TextBlock x:Name="mentionTextbox" VerticalAlignment="Center" Margin="2,0" Text="{Binding ReadState.MentionCount}" Foreground="{ThemeResource ComboBoxItemSelectedForegroundThemeBrush}"/>
                                                </Grid>
                                            </Grid>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                </ListView>

                                <lib:NavigationViewItemSeparator />
                            </StackPanel>
                        </ListView.Header>
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.Footer>
                            <StackPanel Margin="0,0,0,8">
                                <lib:NavigationViewItemSeparator/>
                                <ListViewItem x:Name="settingsItem" Tapped="SettingsItem_Tapped" Padding="0" MinWidth="0" HorizontalContentAlignment="Stretch">
                                    <SymbolIcon Width="36" Height="36" Symbol="Setting"></SymbolIcon>
                                </ListViewItem>
                            </StackPanel>
                        </ListView.Footer>
                    </ListView>
                </Grid>

                <controls1:DropShadowPanel Width="220" Color="{ThemeResource SystemChromeBlackMediumLowColor}" Grid.Column="1">
                    <Grid Grid.Column="1" x:Name="sidebarSecondaryGrid" Background="{ThemeResource SidebarSecondaryAcrylicWindowBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid Height="42">
                            <TextBlock Text="{Binding Content.Tag, ElementName=sidebarFrame, FallbackValue=Unicord}" Grid.Column="1" Margin="16,0" VerticalAlignment="Center" FontSize="18" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                        </Grid>

                        <AutoSuggestBox Grid.Row="1" Style="{StaticResource CleanAutoSuggestBox}" BorderThickness="1" QueryIcon="Find" PlaceholderText="Find some people!" Margin="8,4,8,4"/>

                        <Frame x:Name="sidebarFrame" Width="220" Grid.Row="2"/>
                    </Grid>
                </controls1:DropShadowPanel>
            </Grid>
            <Grid x:Name="content" Grid.Column="1" MinWidth="250" Background="{ThemeResource MainBackgroundBrush}">
                <Grid.RenderTransform>
                    <TranslateTransform x:Name="ContentTransform" />
                </Grid.RenderTransform>
                <Grid>
                    <Frame x:Name="mainFrame" HorizontalAlignment="Stretch" Background="{ThemeResource MainBackgroundBrush}" SourcePageType="sub:FriendsPage" Navigated="mainFrame_Navigated"/>
                </Grid>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition />
                        <ColumnDefinition MinWidth="375" MaxWidth="520"/>
                    </Grid.ColumnDefinitions>

                    <controls1:InAppNotification x:Name="notification" Grid.Column="1" BorderThickness="1"
                                        AnimationDuration="00:00:00.1000000" VerticalAlignment="Top"
                                        HorizontalAlignment="Stretch" Tapped="Notification_Tapped" Padding="16"
                                        Margin="0,24,4,0" ShowDismissButton="True" VerticalOffset="-100" FontSize="{ThemeResource ContentControlFontSize}"
                                        StackMode="StackInFront" Background="{ThemeResource SystemControlAcrylicElementMediumHighBrush}">

                    </controls1:InAppNotification>
                </Grid>
            </Grid>
        </Grid>

        <Grid x:Name="SettingsOverlayGrid" Visibility="Collapsed">
            <Grid x:Name="SettingsOverlayBackground" Background="{ThemeResource OverlayBackdrop}" Opacity="0" Tapped="SettingsOverlayBackground_Tapped"/>
            <Grid x:Name="SettingsContainer" Background="{ThemeResource MainBackgroundBrush}">
                <Grid.RenderTransform>
                    <TranslateTransform x:Name="SettingsPaneTransform" />
                </Grid.RenderTransform>
                <Frame x:Name="SettingsGrid" Grid.Row="1" SourcePageType="settings:SettingsPage"/>
            </Grid>
        </Grid>
    </Grid>
</Page>
